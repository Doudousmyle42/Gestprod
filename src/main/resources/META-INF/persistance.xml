<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
  http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <persistence-unit name="examPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Declare toutes tes entites ici -->
        <class>com.examen.model.Categorie</class>
        <class>com.examen.model.Produit</class>
        <class>com.examen.model.User</class>

        <properties>
            <!-- Driver PostgreSQL -->
            <property name="javax.persistence.jdbc.driver"
                      value="org.postgresql.Driver"/>

            <!-- URL de connexion : remplace 'examdb' par ton nom de BD -->
            <property name="javax.persistence.jdbc.url"
                      value="jdbc:postgresql://localhost:5432/examdb"/>

            <!-- Identifiants PostgreSQL -->
            <property name="javax.persistence.jdbc.user"     value="postgres"/>
            <property name="javax.persistence.jdbc.password" value="ton_mot_de_passe"/>

            <!-- update = Hibernate cree/met a jour les tables automatiquement -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Voir les requetes SQL dans la console -->
            <property name="hibernate.show_sql"   value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Dialecte PostgreSQL -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.PostgreSQLDialect"/>
        </properties>
    </persistence-unit>

</persistence>
